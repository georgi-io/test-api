name: SBT CI

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      REPO_URL: registry.sgeorgi.io
      REPO_IMAGE: test-api
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup JDK
        uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8
      - name: Build and Test
        run: sbt -v +test
